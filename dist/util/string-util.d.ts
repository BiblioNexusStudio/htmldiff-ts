export declare class StringUtil {
    constructor();
    strlen(str: string): number;
    strpos(haystack: string, needle: string, offset?: number): number;
    stripos(haystack: string, needle: string, offset?: number): number;
    substr(str: string, start: number, length?: number | null): string;
}
